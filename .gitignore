# Build output (regenerated by `kas build`)
/build/

# Layers cloned by kas — re-fetched on `kas checkout`
/layers/bitbake/
/layers/openembedded-core/
/layers/meta-yocto/
/layers/meta-raspberrypi/

# uv-managed virtualenv (recreated by `uv sync`)
/.venv/

# Editor / tool state
/.vscode/
/.claude/

# direnv per-project shell env (template at .envrc.example)
/.envrc

# Python / OS scratch
__pycache__/
*.pyc
.DS_Store
